node_modules/
dist/
__pycache__/
*.pyc

# build outputs (generated by build.py)
public/relay.js
public/space.js
public/qr.js

# relay.py / poll.py keep all local state (keys, codes, audit logs) in
# ~/.config/muse-relay with mode 0600 — never in this tree. These are
# belt-and-braces in case state is ever written locally by mistake.
last_seen.json
*.log
